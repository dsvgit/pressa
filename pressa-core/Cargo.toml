[package]
name = "pressa-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
indexmap.workspace = true
# Records are stored as JSON documents — ADR-0003.
serde_json.workspace = true
# `DateTime` fields are RFC 3339 strings and must be parsed to be validated.
chrono.workspace = true
# `Record.id` is a ULID — domain-model.md §7.
ulid.workspace = true
# `StorageError` is a library error type — AGENTS.md.
thiserror.workspace = true
