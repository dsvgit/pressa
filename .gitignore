/target
**/*.rs.bk
Cargo.lock.orig

# Pressa project data
.pressa/
*.db
*.db-wal
*.db-shm

# insta
**/*.snap.new

.DS_Store

# Personal Claude Code overrides (project settings.json IS committed)
.claude/settings.local.json

# Local scratch notes (assessments, drafts) — not for the repo
notes/local/
